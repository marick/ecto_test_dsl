defmodule TransformerTestSupport.Impl.SmartGet.Changeset do
  alias TransformerTestSupport.Impl.Get
    
  @moduledoc """
  """

  # def get(test_data_name_or_value, example_name) do
  #   test_data = Get.test_data(test_data_name_or_value)
  # end
  
#    do: get(Get.test_data(test_data_module), example_name)
  # def get(test_data, example_name) do
  #   example = Get.example(test_data, example_name)
  #   changeset = example.changeset
  #   if example.category == :validation_failure do
      
      
end
